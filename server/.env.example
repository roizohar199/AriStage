# copy to .env and fill
DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=
DB_NAME=ari_stage

JWT_SECRET=super_secret_change_me
HOST=0.0.0.0
PORT=5000

# Client URL for CORS (use LAN-accessible Vite dev server on mobile)
# Replace 10.100.102.99 with your machine's LAN IP
CLIENT_URL=http://10.100.102.99:5173

# Optional: extend allowed CORS origins (comma-separated)
# CORS_EXTRA_ORIGINS=http://localhost:5173,http://10.100.102.99:5173
